---
#Service topic
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mydeploy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: deployment
  template:
    metadata:
      name: testpod8
      labels:
        app: deployment
    spec:
      containers:
      - name: c00
        image: httpd
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 80
