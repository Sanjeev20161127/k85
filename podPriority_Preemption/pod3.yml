kind: Pod
metadata:
  name: app3
  labels:
    env: dev
spec:
  containers:
  - name: app3
    image: nginx
    resources:
      requests:
        memory: "4Gi"
        cpu: "2"
      limits:      
        memory: "4Gi"
        cpu: "2"

# priorityClassName: high-priority