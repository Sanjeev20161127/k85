apiVersion: v1
kind: Pod
metadata:
  name: testPod
  labels:
    name: testPod
spec:
  containers:
   - name: nginx
     image: nginx
     command: ["/bin/bash", "-c", "while true; do echo Hello-Adam; sleep 8 ; done"]
     resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
